export default function ScanPage() {
  return (
    <main className="mx-auto flex min-h-[60vh] w-full max-w-3xl flex-col items-center justify-center px-4 pb-24 pt-6 text-center">
      <h1 className="text-2xl font-semibold tracking-tight">Scan</h1>
      <p className="mt-2 max-w-sm text-sm text-zinc-500">
        Camera OCR lands in Phase 3. For now, use Search to add cards.
      </p>
    </main>
  );
}
